{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  "MyKey": "This value is coming from appsettings.development.json file" 
  // now this will be the output, it got overrode the appsettings.json file
}
